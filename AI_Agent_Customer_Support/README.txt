# Customer Support AI
Note add env file  which include all API and URL
1. Activate:venv\Scripts\activate
2. Install dependencies: `pip install -r requirements.txt`
3. set CHROMADB_TELEMETRY_ENABLED=false
4. Create a `.env` file with: API and URL
6. Run the app: `uvicorn app.main:app --reload`


